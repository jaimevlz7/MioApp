<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jaime.mioapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroActivity" />
        <activity android:name=".InicioSesionActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".EdicionPerfilActivity" />
        <activity android:name=".ConfiguracionActivity" />
        <activity android:name=".RutaVivoActivity" />
        <activity android:name=".PlaneaRutaActivity" />
        <activity android:name=".NoticiasActivity" />
        <activity android:name=".TarjetasActivity" />
        <activity android:name=".OpinionActivity"></activity>
    </application>

</manifest>